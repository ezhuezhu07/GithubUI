import 'package:flutter/material.dart';
import 'package:github_clone/colors.dart';

const String BASEURL = 'https://api.github.com';

const double defaultPadding = 16.0;
const double defaultBorderRadius = 10.0;

const TextStyle titleStyle =
    TextStyle(color: white, fontSize: 16, fontWeight: FontWeight.w600);
const TextStyle subTitleStyle =
    TextStyle(color: white, fontWeight: FontWeight.w500);
const String ACCEPT = 'application/vnd.github+json';
